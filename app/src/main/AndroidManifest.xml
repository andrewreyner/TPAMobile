<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tpa.HelepDoc">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.ViewTransactionActivity"></activity>
        <activity android:name=".main.ProfileActivity" />
        <activity android:name=".main.DoctorActivity"></activity>
        <activity android:name=".main.HomeActivity">

        </activity>
        <activity android:name=".CartActivity" />
        <activity android:name=".ProductDetail" />
        <activity android:name=".ProductPage"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".auth.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".auth.RegisterActivity" />
    </application>

</manifest>